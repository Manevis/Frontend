import React from 'react';
import PropTypes from 'prop-types';

const Subjects = props => {
  return (
    <div>
      Subjects
    </div>
  );
};

Subjects.propTypes = {

};

export default Subjects;
